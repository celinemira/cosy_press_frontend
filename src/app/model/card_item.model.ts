export type cardItem = {
  dimension: number;
  innerRadius: number;
  name: string,
  lavage: boolean,
  repassage: boolean,
  quantity: number,
  total: any|number,
  date: string,
  image: string
};
